{
  "name": "clay",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --filter @clay/shell dev",
    "backend": "pnpm --filter @clay/backend start",
    "codex": "pnpm --filter @clay/backend codex",
    "typecheck": "pnpm --workspace-concurrency=1 -r typecheck",
    "test": "pnpm -r test",
    "test:ordinary": "pnpm --filter @clay/backend test --maxWorkers=1 --minWorkers=1 && pnpm --filter @clay/schema test --maxWorkers=1 --minWorkers=1 && pnpm --filter @clay/kernel test --maxWorkers=1 --minWorkers=1 && pnpm --filter @clay/mutation test --maxWorkers=1 --minWorkers=1 && pnpm --filter @clay/panel-runtime test --maxWorkers=1 --minWorkers=1 && pnpm --filter @clay/shell test --maxWorkers=1 --minWorkers=1 && node --test scripts/bundle-budget.test.mjs",
    "build": "pnpm --filter @clay/shell build",
    "budget": "node scripts/bundle-budget.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm budget",
    "check:ordinary": "pnpm typecheck && pnpm test:ordinary && pnpm build && pnpm budget",
    "verify:shape-map": "node scripts/shape-map.mjs evidence",
    "verify:change-contract": "node scripts/change-contract.mjs evidence",
    "verify:providers": "node scripts/provider-connections.mjs evidence",
    "verify:lazy-boundaries": "node scripts/lazy-boundaries.mjs evidence",
    "verify:operations": "node scripts/connected-operations.mjs evidence",
    "verify:product": "pnpm verify:shape-map && pnpm verify:change-contract && pnpm verify:providers && pnpm verify:lazy-boundaries && pnpm verify:operations",
    "verify:browsers": "node scripts/browsers.mjs",
    "verify:multi-app": "node scripts/multi-app-archive.mjs",
    "regression": "pnpm --filter @clay/mutation regression"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "playwright": "^1.61.1"
  },
  "dependencies": {
    "hono": "^4.12.34",
    "pg": "^8.22.0"
  }
}
